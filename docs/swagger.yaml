info:
  contact: {}
paths:
  /order:
    get:
      consumes:
      - application/json
      description: get all order
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
